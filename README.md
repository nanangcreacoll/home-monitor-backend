# IoT Home Monitoring System - Backend

## Database Seeding

This project includes a simple, idempotent database seeder for local/dev usage.

### Run the seeder

Set the `SEED` environment variable to `true` when starting the application.